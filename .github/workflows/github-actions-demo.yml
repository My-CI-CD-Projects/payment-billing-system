name: My first GitHub Actions

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17 for x64
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          architecture: x64

  deployment:
    runs-on: ubuntu-latest
    environment: DEV
    needs: build  # Ensures the deployment job runs after the build job
    steps:
      - name: Deploy
        run: echo "Deploying to DEV environment..."  # Replace with actual deployment commands
